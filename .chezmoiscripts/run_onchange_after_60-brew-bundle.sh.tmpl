#!/bin/bash
# Brewfile hash: {{ include "Brewfile" | sha256sum }}
# Re-runs whenever the Brewfile changes, not just once.
set -euo pipefail

eval "$(/opt/homebrew/bin/brew shellenv)"

brew bundle --file="{{ .chezmoi.sourceDir }}/Brewfile"
